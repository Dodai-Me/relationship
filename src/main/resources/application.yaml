spring:
  application:
    name: relationship

  datasource:
    url: jdbc:mysql://localhost:3306/banking_system
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
